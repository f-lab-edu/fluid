//gradle 이 빌드할 때 가장 먼저 읽는 파일
// 프로젝트에 포함시킬 하위 모듈을 gradle 에게 알려줌
rootProject.name = 'fluid'
include 'app'
include 'core'
include 'util'

//Q. 멀티 모듈로 build하면 나누면 어떤 장점이 있나?
//단순히 기능 구분이면 패키지만 나누면 안되나?

//JDK 설치하는 세팅